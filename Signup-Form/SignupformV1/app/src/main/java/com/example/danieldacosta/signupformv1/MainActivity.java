package com.example.danieldacosta.signupformv1;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.Button;
import android.widget.EditText;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        EditText email_edittext = (EditText) findViewById(R.id.email_edittext)
        EditText password_edittext = (EditText) findViewById(R.id.password_edittext)
        Button CreateButtonButton = (Button) findViewById(R.id.Create_account)
        Button CreateButtonButton = (Button) findViewById(R.id.login_button)




    }
}
